# Backlog

## E2E

- [ ] Make `e2e/start.sh` work on environments that only provide Docker Compose v2 (`docker compose`) instead of the legacy `docker-compose` binary.
  Current failure:
  `./start.sh: line 4: docker-compose: command not found`
